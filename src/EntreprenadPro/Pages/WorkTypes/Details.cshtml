@page
@model EntreprenadPro.Pages.WorkTypes.DetailsModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>

<div>
    <h4>WorkType</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.WorkType.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.WorkType.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.WorkType.HourlyRate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.WorkType.HourlyRate)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.WorkType.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
</body>
</html>
